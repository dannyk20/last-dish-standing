# Restaurant Survival 제약사항

## 1. 개발 범위

본 프로젝트는 하루 안에 구현하는 단기 프로젝트이다.

기능 수보다 핵심 게임 루프의 완성도를 우선한다.

최우선 사용자 흐름은 다음과 같다.

```text
지역 + 음식 선택
        ↓
Google Places 검색
        ↓
후보 품질 검증
        ↓
2~8개 참가 식당 결정
        ↓
별명 + 한 줄 소개 생성
        ↓
게임 데이터 메모리 저장
        ↓
두 식당 비교
        ↓
사용자 선택
        ↓
평점 공개
        ↓
승자 생존
        ↓
다음 도전자
        ↓
필요 시 패자부활전
        ↓
Winner
```

위 흐름과 직접적인 관련이 없는 기능은 추가하지 않는다.

---

## 2. 구현 환경

다음 기술을 기본으로 사용한다.

- React
- TypeScript
- Vite
- Google Places API
- HTML
- CSS

스타일링 방식은 하나로 통일한다.

불필요한 라이브러리를 추가하지 않는다.

---

## 3. Google Places 데이터

실제 식당 정보는 Google Places API를 사용한다.

다음 정보 확보를 목적으로 한다.

- 식당명
- 카테고리
- 주소
- 대표 사진
- 가격대
- 평점
- 리뷰 수
- 리뷰
- Google Maps 링크

Google Places에서 제공하지 않는 실제 식당 정보를 임의로 생성하지 않는다.

---

## 4. 후보 품질 제약

Google Places 검색 결과를 그대로 게임 참가자로 사용하지 않는다.

다음 세 가지를 핵심 비교 정보로 정의한다.

1. 대표 사진
2. 리뷰
3. 가격대

세 가지 중 최소 2개 이상을 확보한 식당만 게임 참가 후보로 사용할 수 있다.

```text
3개 확보 → 참가 가능
2개 확보 → 참가 가능
1개 확보 → 참가 불가
0개 확보 → 참가 불가
```

식당 수를 채우기 위해 품질 조건을 완화하지 않는다.

8개의 참가자를 만드는 것보다 사용자가 비교할 수 있는 충분한 정보가 존재하는 것을 우선한다.

---

## 5. 동적 게임 규모

참가 식당 수를 8개로 강제하지 않는다.

최대 참가 식당 수는 8개이다.

최소 참가 식당 수는 2개이다.

품질 조건을 만족한 식당이 2~7개라면 해당 식당만으로 게임을 진행한다.

```text
8개 이상 → 8개
7개 → 7개
6개 → 6개
5개 → 5개
4개 → 4개
3개 → 3개
2개 → 2개
1개 이하 → 게임 시작 불가
```

후보 부족을 이유로 품질 조건을 만족하지 않는 식당을 추가해서는 안 된다.

---

## 6. 동적 라운드 제약

일반 대결 횟수는 다음 규칙으로 계산한다.

```text
일반 대결 수 = 참가 식당 수 - 1
```

참가 식당 수나 전체 일반 라운드 수를 코드에 `8`, `7` 등으로 하드코딩하지 않는다.

게임 로직은 2~8개의 모든 참가자 수에서 동일하게 동작해야 한다.

---

## 7. 패자부활전 활성화 제약

패자부활전은 실제 참가 식당 수를 기준으로 활성화한다.

```text
참가 식당 6개 → 패자부활전 O
참가 식당 7개 → 패자부활전 O
참가 식당 8개 → 패자부활전 O

참가 식당 2개 → 패자부활전 X
참가 식당 3개 → 패자부활전 X
참가 식당 4개 → 패자부활전 X
참가 식당 5개 → 패자부활전 X
```

이를 다음 조건으로 구현한다.

```ts
const canUseRevival = restaurants.length >= 6;
```

단, 실제 변수명이나 구현 구조는 프로젝트 설계에 맞게 변경할 수 있다.

패자부활전은 한 게임에서 최대 한 번만 실행한다.

---

## 8. 패자부활 시점

패자부활전은 최소 3개의 식당이 탈락한 이후에만 실행한다.

6~8명의 참가자가 존재하더라도 3명의 탈락자가 생기기 전에 패자부활전을 실행하지 않는다.

8개 참가의 경우 4번째 일반 대결 이후 실행하는 것을 기본으로 한다.

6~7개 참가의 경우 최소 3개의 탈락자가 발생한 이후 적절한 시점에 실행한다.

패자부활전 실행 여부와 시점은 게임 시작 시 결정된 참가 식당 수를 기준으로 판단한다.

---

## 9. 패자부활 데이터 제약

패자부활전을 위해 Google Places API를 새로 호출하지 않는다.

현재 게임의 탈락 식당 데이터를 그대로 사용한다.

패자부활 후보는 최대 3개이다.

부활 식당이 다시 패배해도 두 번째 부활 기회를 제공하지 않는다.

사용자는 패자부활전을 건너뛸 수 있어야 한다.

---

## 10. API 호출 및 캐싱 제약

Google Places API 호출 횟수를 최소화한다.

Text Search, Place Details, Place Photo 등의 요청을 각 라운드에서 반복 수행하지 않는다.

기본 데이터 흐름은 다음과 같다.

```text
Google Places API
        ↓
식당 검색
        ↓
필요한 식당 정보 확보
        ↓
후보 품질 검증
        ↓
최대 8개 참가자 결정
        ↓
Restaurant 데이터 생성
        ↓
현재 게임 메모리에 저장
        ↓
게임 시작
        ↓
API 재호출 없이 게임 진행
```

다음 원칙을 반드시 지킨다.

- 게임 시작 시 필요한 데이터를 가능한 범위에서 확보한다.
- 동일 Place ID에 대해 같은 정보를 불필요하게 반복 요청하지 않는다.
- 매 라운드마다 식당 검색 API를 호출하지 않는다.
- 평점 공개 단계에서 API를 다시 호출하지 않는다.
- 다음 대결 진행 시 API를 다시 호출하지 않는다.
- 패자부활전에서 API를 다시 호출하지 않는다.
- Winner 화면에서 API를 다시 호출하지 않는다.
- 별명 생성 때문에 API를 추가 호출하지 않는다.
- 한 줄 소개 생성 때문에 API를 추가 호출하지 않는다.
- 현재 게임에서 확보한 Restaurant 데이터를 메모리에서 재사용한다.

---

## 11. Google Places 요청 필드

실제 화면과 게임 로직에서 사용하는 데이터만 요청한다.

필요하지 않은 데이터를 무분별하게 요청하지 않는다.

Google Places API의 Field Mask를 사용할 수 있는 요청에서는 필요한 필드만 지정한다.

API 사용량과 구현 복잡도를 불필요하게 증가시키지 않는다.

---

## 12. API Key 보안

Google Maps Platform API Key를 코드에 문자열로 직접 하드코딩하지 않는다.

개발 환경에서는 환경 변수로 관리한다.

```text
VITE_GOOGLE_MAPS_API_KEY
```

`.env` 파일은 Git 저장소에 커밋하지 않는다.

필요한 경우 `.env.example`만 저장소에 포함한다.

단, Vite의 `VITE_` 환경 변수는 빌드된 프론트엔드 애플리케이션에서 접근 가능한 값이다.

따라서 `.env`에 저장하는 것만으로 API Key가 사용자에게 숨겨지는 것은 아니다.

Google Cloud Console에서 API Key에 제한을 설정한다.

최소 다음 제한을 적용한다.

- 허용된 웹사이트만 사용할 수 있도록 HTTP referrer 제한
- 실제 사용하는 Google Maps Platform API만 허용하도록 API 제한
- 개발 중 필요한 localhost만 허용
- 배포 시 실제 배포 도메인 추가
- 사용하지 않는 Google API 호출 제한

API Key를 프론트엔드에서 완전히 숨겨진 비밀 값으로 간주하지 않는다.

---

## 13. Survival 별명 생성

`survivalTitle`은 Restaurant Survival의 게임용 표현이다.

식당의 공식 별명으로 표현하지 않는다.

별명 생성에는 다음 정보만 사용한다.

- 음식 카테고리
- 지역
- 가격대
- Google Places 리뷰
- 리뷰에서 확인 가능한 특징

확인되지 않은 특징을 임의로 생성하지 않는다.

---

## 14. 별명 표현 제약

별명은 6~18자 내외를 권장한다.

예:

```text
성수의 와인 강자
데이트 코스 강자
가성비 파스타 사냥꾼
분위기로 승부하는 한 끼
```

다음과 같은 사실 확인이 필요한 표현은 사용하지 않는다.

```text
서울 최고의 식당
성수 1위
대한민국 최고의 파스타
미슐랭급 레스토랑
유명 셰프의 식당
```

게임성을 위해 표현을 재미있게 만들 수 있지만 사실 관계를 왜곡해서는 안 된다.

---

## 15. 한 줄 소개 생성

`survivalSummary`는 공개 데이터에서 확인할 수 있는 특징을 요약한다.

한 문장 정도로 작성한다.

예:

```text
분위기와 데이트 관련 호평이 많은 프렌치 레스토랑
```

다음 내용을 임의로 생성하지 않는다.

- 존재하지 않는 메뉴
- 존재하지 않는 서비스
- 확인되지 않은 시설
- 확인되지 않은 셰프 정보
- 확인되지 않은 수상 기록

---

## 16. 별명 및 소개 생성 방식

하루 프로젝트에서는 규칙 기반 생성을 기본으로 한다.

별명과 소개 생성을 위해 별도의 AI API를 필수로 사용하지 않는다.

리뷰에서 간단한 키워드를 탐색할 수 있다.

예:

```text
분위기
데이트
가성비
친절
웨이팅
와인
파스타
디저트
뷰
조용
```

키워드와 카테고리, 지역, 가격대 등을 조합하여 미리 정의된 템플릿에서 별명과 소개를 생성한다.

---

## 17. 별명 fallback

특징을 판단하기 어려운 경우 억지로 강한 별명을 생성하지 않는다.

예:

```text
오늘의 도전자
새로운 맛의 도전자
지역의 한 끼 후보
```

한 줄 소개도 정보가 부족하면 안전한 표현을 사용한다.

```text
현재 검색 조건에 맞는 식당 후보입니다.
```

---

## 18. 공식 정보와 게임용 표현 구분

Survival 별명과 한 줄 소개가 Google 또는 해당 식당의 공식 설명처럼 보이지 않도록 한다.

서비스 내 적절한 위치에 다음과 같은 안내를 제공한다.

```text
Restaurant Survival이 공개된 식당 정보를 바탕으로 만든 게임용 표현입니다.
```

모든 카드에 반복해서 표시할 필요는 없다.

---

## 19. 평점 공개 제약

선택 전에는 평점을 표시하지 않는다.

선택 전:

```text
별명
사진
식당명
카테고리
위치
가격대
한 줄 소개
대표 리뷰
```

선택 후:

```text
위 정보
+
평점
리뷰 수
평점 비교 메시지
```

평점은 CSS로 단순히 숨기는 것보다 선택 전 컴포넌트에서 렌더링하지 않는 방식을 우선한다.

---

## 20. 승패 결정

평점은 승패에 영향을 주지 않는다.

승자는 반드시 사용자의 선택으로 결정한다.

```text
A ⭐ 4.2
B ⭐ 4.8

사용자 → A 선택

결과 → A 승리
```

---

## 21. 리뷰 사용

Google Places API를 통해 제공받은 리뷰만 사용한다.

다음 행위를 하지 않는다.

- 네이버 플레이스 리뷰 크롤링
- 카카오맵 리뷰 크롤링
- 캐치테이블 리뷰 크롤링
- 기타 플랫폼 리뷰 크롤링
- 가짜 사용자 리뷰 생성

---

## 22. 데이터 누락 처리

Google Places API에서 다음 정보가 누락될 수 있다고 가정한다.

- 사진
- 가격대
- 리뷰
- 평점
- 리뷰 수
- 주소

데이터 일부가 누락되어도 후보 품질 조건을 만족한다면 게임을 진행할 수 있다.

각 데이터에는 fallback UI를 제공한다.

---

## 23. 게임 상태 관리

게임 상태는 클라이언트에서 관리한다.

예:

```ts
type GameStatus =
  | "setup"
  | "loading"
  | "playing"
  | "ratingReveal"
  | "revival"
  | "finished"
  | "error";
```

별명과 한 줄 소개는 게임 시작 전에 Restaurant 데이터에 포함한다.

Redux 등의 별도 전역 상태 관리 라이브러리를 필수로 사용하지 않는다.

React 기본 상태 관리 기능으로 해결 가능한 범위를 우선한다.

---

## 24. 개인정보

다음 사용자 정보를 수집하지 않는다.

- 이름
- 이메일
- 전화번호
- 생년월일
- 계정 정보

로그인 및 회원가입을 구현하지 않는다.

GPS 기반 현재 위치를 자동 수집하지 않는다.

사용자가 직접 지역을 지정한다.

---

## 25. 서버 및 데이터베이스

다음 기능을 위한 별도의 데이터베이스를 구축하지 않는다.

- 회원
- 사용자 프로필
- 게임 기록
- 별명
- 리뷰
- 취향 데이터

게임 진행 상태는 현재 클라이언트 세션에서 관리한다.

---

## 26. 디자인 방향

Restaurant Survival은 식당들이 경연 참가자처럼 경쟁하는 분위기를 지향한다.

특정 방송 프로그램의 로고, 그래픽, 고유 UI 등을 직접 복제하지 않는다.

사용 가능한 표현:

```text
SURVIVAL TITLE
CURRENT CHAMPION
CHALLENGER
WIN STREAK
RATING REVEAL
REVIVAL ROUND
FINAL
WINNER
```

식당 대표 사진과 Survival 별명을 주요 시각 요소로 사용한다.

---

## 27. 반응형

PC와 모바일을 지원한다.

최소 화면 폭은 360px이다.

PC에서는 두 식당을 좌우로 배치한다.

모바일에서는 세로 배치를 허용한다.

---

## 28. 애니메이션

가벼운 CSS transition 또는 animation만 사용한다.

가능한 연출:

- 카드 등장
- 별명 등장
- 선택 카드 강조
- 탈락 카드 fade-out
- 평점 reveal
- Revival 등장
- Winner 등장

WebGL이나 복잡한 3D 효과를 사용하지 않는다.

---

## 29. 구현 제외 범위

다음 기능은 구현하지 않는다.

- 로그인
- 회원가입
- 소셜 로그인
- 사용자 인증
- 사용자 프로필
- 관리자 화면
- 실제 데이터베이스
- AI 맛집 추천
- 머신러닝 추천
- AI 리뷰 분석
- AI 리뷰 요약
- LLM 기반 별명 생성 필수화
- 자체 리뷰 작성
- 댓글
- 좋아요
- 식당 예약
- 예약 가능 시간
- 결제
- 배달
- 메뉴 주문
- 전체 메뉴판
- 메뉴별 실제 가격
- 실시간 웨이팅
- 실시간 좌석
- GPS 실시간 위치
- 길찾기
- 친구 초대
- 멀티플레이
- 실시간 대전
- 웹 크롤링
- 특정 방송 프로그램 UI 직접 복제

Kiro는 위 기능을 임의로 추가하지 않는다.

---

## 30. 구현 우선순위

하루 안에 완성하기 위해 다음 순서로 구현한다.

1. 프로젝트 기본 구조
2. 음식 종류 선택
3. 지역 입력
4. Google Places API 연동
5. API 응답 변환
6. 후보 품질 검증
7. 동적 참가자 수 결정
8. 식당 데이터 메모리 저장
9. Survival 별명 생성
10. 한 줄 소개 생성
11. 식당 카드
12. 두 식당 비교
13. 선택 전 평점 숨김
14. 식당 선택
15. 평점 공개
16. 평점 비교 메시지
17. Survival 생존/탈락
18. 동적 라운드 처리
19. Winner
20. 오류 및 fallback
21. 패자부활전 조건 분기
22. 패자부활전
23. 반응형
24. 애니메이션

앞 단계가 정상적으로 동작하기 전에 후순위 기능을 먼저 구현하지 않는다.

---

## 31. 최우선 완료 범위

시간이 부족한 경우 다음 흐름을 반드시 먼저 완성한다.

```text
지역 + 음식 선택
        ↓
Google Places 검색
        ↓
후보 품질 검증
사진 / 리뷰 / 가격대 중 2개 이상
        ↓
2~8개 참가자 결정
        ↓
별명 + 한 줄 소개
        ↓
Restaurant 데이터 메모리 저장
        ↓
A VS B
        ↓
사용자 선택
        ↓
평점 공개
        ↓
승자 VS 다음 도전자
        ↓
동적 라운드 진행
        ↓
Winner
```

패자부활전과 애니메이션보다 위 핵심 게임 루프를 먼저 완성한다.

**미완성된 많은 기능보다 처음부터 Winner까지 정상적으로 플레이 가능한 하나의 완성된 흐름을 우선한다.**